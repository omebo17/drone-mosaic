export const environment = {
  production: true,
  /**
   * Canonical URL for SEO (no trailing slash). Must match live domain, sitemap, and robots Sitemap line.
   * Change if you deploy elsewhere (e.g. GitHub Pages: use that origin or leave '' to derive from window).
   */
  siteUrl: 'https://photon.ge'
};
