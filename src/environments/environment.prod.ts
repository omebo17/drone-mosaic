export const environment = {
  production: true,
  /**
   * Canonical URL for SEO (no trailing slash). Example: https://photon.ge
   * Leave empty to build URLs from the browser origin and base href (e.g. GitHub Pages).
   */
  siteUrl: ''
};
